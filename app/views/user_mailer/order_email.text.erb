Nouvelle commande de <%= @user.first_name unless @user.first_name.empty? %>

Voici les détails de la commande :

<% @order.order_items.each do |oi| %>
- <%= oi.item.title %> (<%= oi.item.price %> €)
<% end %>
Total (<%= @order.items.count %> photos): <%= @order.total_price %> €

Pour accéder au commandes, connecte-toi via :
<%= @url %>
